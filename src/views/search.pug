extends base
include mixins/video

block content 
    form(method="GET")
        input(placeholder="Search by title", name="keyword", tpye="text")
        input(type="submit", value="serach now")

        each video in videos 
            +video(video)