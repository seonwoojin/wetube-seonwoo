extends ../base
include ../mixins/video

block content 
    .profile__data 
        img(src="/" + user.avatarUrl).avatar
        h3=user.name
    div.video-grid
        each video in user.videos
            +video(video)
        else 
            sapn.empry__message No videos found